# Logic simulation on GPU for ECE757 benchmark

# Clone
```
git clone -b ECE757 https://github.com/Yi-Huaaa/LSIM.git
```

## Build and run
```
~$ mkdir build
~$ cp run.sh build/
~$ cd build
~$ cmake ../
~$ make -j
~$ bash run.sh
```
